---
import { getPermalink } from '~/utils/permalinks';

interface Course {
  slug: string;
  title: string;
  description: string;
  bullets: string[];
  buttonText: string;
}

interface Props {
  title: string;
  courses: Course[];
  featuredSlugs?: string[];
}

const { title, courses, featuredSlugs = [] } = Astro.props;
const cardClass =
  'flex h-full flex-col rounded-lg border border-gray-200 bg-white p-6 shadow-sm dark:border-gray-700 dark:bg-slate-800';
---

<div>
  <h3 class="text-2xl font-bold text-gray-950 dark:text-white">{title}</h3>
  <div class="mt-5 grid gap-5 md:grid-cols-2 xl:grid-cols-3">
    {
      courses.map((course) => (
        <article
          class:list={[cardClass, featuredSlugs.includes(course.slug) && 'border-primary ring-2 ring-primary/15']}
        >
          {featuredSlugs.includes(course.slug) && (
            <p class="mb-3 text-xs font-bold uppercase tracking-wide text-secondary dark:text-teal-200">
              Popular GCSE support
            </p>
          )}
          <h3 class="text-xl font-bold text-gray-950 dark:text-white">{course.title}</h3>
          <p class="mt-3 text-base leading-7 text-muted dark:text-slate-300">{course.description}</p>
          <ul class="mt-4 space-y-2 text-base leading-7 text-muted dark:text-slate-300">
            {course.bullets.map((item) => (
              <li class="flex gap-2">
                <span class="mt-2 size-1.5 rounded-full bg-secondary" aria-hidden="true" />
                <span>{item}</span>
              </li>
            ))}
          </ul>
          <div class="mt-auto pt-5">
            <a class="btn-secondary w-full justify-center sm:w-auto" href={getPermalink(`/${course.slug}`)}>
              {course.buttonText}
            </a>
          </div>
        </article>
      ))
    }
  </div>
</div>
